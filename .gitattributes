# Files that Linux reads must keep LF line endings even when they are checked out on Windows
# (with core.autocrlf a shell script gets CRLF and then fails with "bad interpreter").
*.sh text eol=lf
*.rules text eol=lf
*.desktop text eol=lf
*.service text eol=lf
*.dll binary
*.lib binary
